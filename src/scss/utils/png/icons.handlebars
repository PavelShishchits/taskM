{{#block "spritesheet"}}
[class*='png-']{
    display: inline-block;
    &:before {
        @include png;
    }
}
{{/block}}

{{#each sprites}}
.png-{{name}}:before {
    @include png-{{name}};
}
{{/each}}